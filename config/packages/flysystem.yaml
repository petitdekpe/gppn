flysystem:
    storages:
        download.storage:
            adapter: 'local'
            options:
                directory: '%kernel.project_dir%/public/uploads/downloads'
            public_url: '/uploads/downloads'
