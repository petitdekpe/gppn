<a href="{{ path('app_langue_show', {slug: row.language.slug}) }}" class="tile tile--language">
    <span class="tile__name">{{ row.language.name }}</span>
    <span class="tile__count">{{ row.videoCount }} vidéo{{ row.videoCount > 1 ? 's' : '' }}</span>
</a>
