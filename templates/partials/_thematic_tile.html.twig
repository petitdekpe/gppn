<a href="{{ path('app_thematique_show', {slug: row.thematic.slug}) }}" class="tile tile--thematic" style="background-color: {{ row.thematic.colorHex }}">
    <span class="tile__name">{{ row.thematic.name }}</span>
    <span class="tile__count">{{ row.videoCount }} contenu{{ row.videoCount > 1 ? 's' : '' }}</span>
</a>
