# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=8f06c0dea38ccb78a62dbf7303e822ff
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
# Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
# See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> doctrine/doctrine-bundle ###
# Format described at https://www.doctrine-project.org/projects/doctrine-dbal/en/latest/reference/configuration.html#connecting-using-a-url
# IMPORTANT: You MUST configure your server version, either here or in config/packages/doctrine.yaml
#
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data_%kernel.environment%.db"
DATABASE_URL="mysql://root:@127.0.0.1:3306/gppn?serverVersion=9.1.0&charset=utf8mb4"
# DATABASE_URL="mysql://app:!ChangeMe!@127.0.0.1:3306/app?serverVersion=10.11.2-MariaDB&charset=utf8mb4"
# DATABASE_URL="postgresql://app:!ChangeMe!@127.0.0.1:5432/app?serverVersion=16&charset=utf8"
###< doctrine/doctrine-bundle ###

###> symfony/messenger ###
# Choose one of the transports below
# MESSENGER_TRANSPORT_DSN=amqp://guest:guest@localhost:5672/%2f/messages
# MESSENGER_TRANSPORT_DSN=redis://localhost:6379/messages
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
###< symfony/messenger ###

###> symfony/mailer ###
MAILER_DSN=null://null
###< symfony/mailer ###

###> app/video-pipeline ###
# Chemins vers les binaires FFmpeg/FFprobe utilisés par le pipeline de
# traitement des capsules vidéo (vignette, transcodage HLS, durée). Laisser
# le nom seul (résolu via le PATH) ou renseigner un chemin absolu.
FFMPEG_BINARY=ffmpeg
FFPROBE_BINARY=ffprobe
# 1 : le pipeline publie automatiquement la capsule une fois le traitement
# terminé. 0 : la capsule passe en "En relecture" pour validation manuelle.
VIDEO_PIPELINE_AUTO_PUBLISH=1
# DSN Redis utilisé pour le transport Messenger "async" en production
# uniquement (voir config/packages/messenger.yaml, bloc when@prod). Sans objet
# en dev, qui utilise le transport Doctrine ci-dessus.
# REDIS_MESSENGER_DSN=redis://localhost:6379/messages
###< app/video-pipeline ###

###> app/video-downloads (minio) ###
# Identifiants et bucket MinIO/S3 utilisés pour stocker le fichier de
# téléchargement des capsules (champ downloadFile sur Video). En prod, ces
# valeurs pointent vers un vrai bucket S3 et sont surchargées via
# .env.prod.local (ne jamais committer de vrais secrets ici).
MINIO_ROOT_USER=gppn
MINIO_ROOT_PASSWORD=changeme-in-local-env
MINIO_BUCKET=gppn-videos
MINIO_REGION=us-east-1
MINIO_ENDPOINT=http://127.0.0.1:9000
MINIO_PUBLIC_ENDPOINT=http://127.0.0.1:9000
###< app/video-downloads (minio) ###
